export const SFS_THEME = {
  brand: {
    black: "#0a0a0a",
    brown: "#5c4033",
    gold: "#d4af37"
  },
  colors: {
    background: "#0a0a0a",
    surface: "#17110d",
    primary: "#d4af37",
    accent: "#8b5e34",
    text: "#f5f1e8"
  }
};

export const sfsTheme = SFS_THEME;
export const smartFlowTheme = SFS_THEME;
export const brandTheme = SFS_THEME;
export const theme = SFS_THEME;

export function applySfsTheme() {
  return SFS_THEME;
}

export function applySFSTheme() {
  return SFS_THEME;
}

export function applySmartFlowTheme() {
  return SFS_THEME;
}

export function initSfsTheme() {
  return SFS_THEME;
}

export function initSFSTheme() {
  return SFS_THEME;
}

export function initSmartFlowTheme() {
  return SFS_THEME;
}

export function setSfsTheme() {
  return SFS_THEME;
}

export function getSfsTheme() {
  return SFS_THEME;
}

export function loadSfsTheme() {
  return SFS_THEME;
}

export function setupSfsTheme() {
  return SFS_THEME;
}

export function injectSfsTheme() {
  return SFS_THEME;
}

export default SFS_THEME;
