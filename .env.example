# SmartFlow Systems - SFS-Backend Environment Configuration Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# Add .env to .gitignore if not already included

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=development
PORT=5000

# ===========================================
# Database Configuration (Drizzle ORM)
# ===========================================
# PostgreSQL (Neon) - Required for this project
# Get your connection string from https://neon.tech
DATABASE_URL=postgresql://username:password@host.neon.tech/database?sslmode=require

# Alternative: Local PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/sfs_backend

# ===========================================
# Session Configuration
# ===========================================
# Session secret for express-session (generate a random string)
SESSION_SECRET=your-session-secret-here-change-this-to-a-random-string

# Session name
SESSION_NAME=sfs.sid

# Session cookie max age in milliseconds (default: 7 days)
SESSION_MAX_AGE=604800000

# ===========================================
# Authentication & Security
# ===========================================
# SFS JWT Secret — SAME value must be set in ALL SFS repos that use sfs-auth.ts
# Generate one: openssl rand -hex 64
SFS_JWT_SECRET=your-sfs-jwt-secret-here

# SFS Backend URL — used by other SFS repos to verify product access
SFS_BACKEND_URL=https://your-sfs-backend.replit.app

# OAuth providers (if used)
# GOOGLE_CLIENT_ID=your-google-oauth-client-id
# GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# ===========================================
# Replit Configuration
# ===========================================
# Automatically set by Replit - used for production detection
# REPL_ID=your-repl-id-automatically-set-by-replit
# REPL_SLUG=your-repl-slug
# REPL_OWNER=your-replit-username

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# ===========================================
# API Keys & Integrations
# ===========================================
# Add any external API keys your backend services need
# EXTERNAL_API_KEY=your-external-api-key-here

# ===========================================
# Logging & Monitoring
# ===========================================
# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable debug mode
DEBUG=false

# ===========================================
# Frontend Build Configuration
# ===========================================
# These are typically auto-configured, but can be customized
# VITE_API_URL=http://localhost:5000/api
